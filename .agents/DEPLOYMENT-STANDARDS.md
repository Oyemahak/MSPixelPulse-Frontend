# Deployment Standards

Do not deploy unless explicitly requested. Before deployment confirm build, tests, env variable names, secrets are not exposed, rollback path, health checks, production URLs, and approval. After deployment verify public URLs, login or key flows, API health, console/network errors, and handoff notes.
