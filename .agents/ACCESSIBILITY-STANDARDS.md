# Accessibility Standards

Target WCAG 2.2 AA where practical. Review semantic HTML, headings, keyboard navigation, focus states, labels, form errors, contrast, alt text, reduced motion, touch targets, screen-reader support, dialogs, menus, and status messages. Separate automated checks from manual checks and limitations.
