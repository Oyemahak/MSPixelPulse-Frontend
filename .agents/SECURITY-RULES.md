# Security Rules

Never expose secrets, commit .env files, display tokens, weaken auth, add public admin bypasses, add unrestricted seed endpoints, log sensitive request bodies, or delete production data without approval. Review authentication, authorization, role enforcement, input validation, uploads, CORS, cookies, JWT, logs, dependencies, debug routes, seed routes, and admin actions.
